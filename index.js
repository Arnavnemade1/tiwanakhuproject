import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

Make sure you have a `public/index.html` file:

<antArtifact identifier="index-html" type="application/vnd.ant.code" language="html" title="public/index.html">
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Tiwanakhu Culture Project</title>
  </head>
  <body>
    <div id="root"></div>
  </body>
</html>
```

Then run:
```bash
npm start
```

This should resolve any startup issues. Would you like me to help you develop the full website for the Tiwanakhu culture project?
